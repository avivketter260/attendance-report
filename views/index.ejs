<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Attendance Report Generator</title>
  <link href="/css/styles.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-gray-100 via-white to-blue-100 min-h-screen flex items-center justify-center">
  <main class="max-w-md mx-auto mt-10 bg-white p-8 rounded-lg shadow-lg">
    <h1 class="text-3xl font-extrabold mb-6 text-center bg-clip-text text-transparent bg-gradient-to-r from-blue-500 to-teal-400">
      Generate Attendance Report
    </h1>
    
    <% if (message) { %>
      <div role="alert" class="text-center text-green-600 font-semibold mb-4"><%= message %></div>
    <% } %>
    
    <form action="/generate" method="POST" class="space-y-6">
      <div class="form-group">
        <label for="month" class="block text-gray-800 font-medium">Month:</label>
        <input 
          type="number" 
          id="month" 
          name="month" 
          min="1" 
          max="12" 
          value="<%= currentMonth %>" 
          required 
          class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div class="form-group">
        <label for="year" class="block text-gray-800 font-medium">Year:</label>
        <input 
          type="number" 
          id="year" 
          name="year" 
          min="1900"
          value="<%= currentYear %>" 
          required 
          class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
      </div>
      
      <div class="form-group">
        <label for="sickDays" class="block text-gray-800 font-medium">Sick Days:</label>
        <input 
          type="text" 
          id="sickDays" 
          name="sickDays" 
          pattern="\d+(?:,\d+)*"
          placeholder="e.g., 16,17" 
          class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          title="Enter comma-separated numbers">
      </div>
      
      <div class="form-group">
        <label for="daysOff" class="block text-gray-800 font-medium">Days Off:</label>
        <input 
          type="text" 
          id="daysOff" 
          name="daysOff" 
          pattern="\d+(?:,\d+)*"
          placeholder="e.g., 5,26" 
          class="w-full mt-2 p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"
          title="Enter comma-separated numbers">
      </div>
      
      <button 
        type="submit" 
        class="w-full bg-blue-500 text-white py-3 rounded-lg hover:bg-blue-600 transition duration-300 transform hover:scale-105 shadow-md">
        Generate CSV
      </button>
    </form>
  </main>
</body>
</html>
